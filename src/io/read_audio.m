function [audio, fs] = read_audio(wav_path, ref_channel_index)
% read_audio
% read a wav file and select the desired channel (reference mic).
% % this is a placeholder stub. real logic will be added in later prompts.

%
% inputs
%   wav_path: char/string path to audio, or [audio, fs] pair in future overload
%   ref_channel_index: optional scalar channel index
%
% outputs
%   audio: column vector (double), selected channel
%   fs: sample rate in hz

%#ok<*INUSD>
audio = [];
fs = [];
end
