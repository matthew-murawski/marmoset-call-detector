function [medv, madv] = rolling_stats(x, fs, window_sec)
% rolling_stats
% rolling median and mad over a sliding window.
% % this is a placeholder stub. real logic will be added in later prompts.

%
% inputs
%   x: 1 x time row vector
%   fs: frames per second (feature rate)
%   window_sec: window length in seconds
%
% outputs
%   medv: rolling median
%   madv: rolling mad

%#ok<*INUSD>
medv = [];
madv = [];
end
