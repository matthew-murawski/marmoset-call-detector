function active = apply_hysteresis(score_hi, score_lo, release_frames)
% apply_hysteresis
% two-state hysteresis: enter on high gate, exit on low gate with hold.
% % this is a placeholder stub. real logic will be added in later prompts.

%
% inputs
%   score_hi: 1 x time boolean for high gate
%   score_lo: 1 x time boolean for low gate
%   release_frames: integer frames to stay active after low gate clears
%
% output
%   active: 1 x time boolean activity state

%#ok<*INUSD>
active = [];
end
