function ev = frames_to_events(active_frames, fs_frames, min_event_ms, merge_gap_ms, max_event_ms)
% frames_to_events
% convert a boolean activity vector into merged onset/offset events.
% % this is a placeholder stub. real logic will be added in later prompts.

%
% inputs
%   active_frames: 1 x time logical
%   fs_frames: frames per second (feature rate)
%   min_event_ms, merge_gap_ms, max_event_ms: constraints
%
% output
%   ev: table with on/off/dur (seconds) and other fields later

%#ok<*INUSD>
ev = table();
end
